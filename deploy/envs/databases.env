# --- PERSISTENCE LAYER (Result Databases) ---
# Postgres (Main Result DB)
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=flight_db

# HBase
HBASE_UI_PORT=16010
HBASE_ZK_PORT=2181

# CockroachDB
COCKROACH_PORT=26257
COCKROACH_HTTP_PORT=8082

# Note: Redis Output uses port 6380 (mapped from 6379 in Docker command)
