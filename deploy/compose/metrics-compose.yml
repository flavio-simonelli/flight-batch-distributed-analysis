name: flight-analytics-metrics

services:
  redis-metrics:
    image: redis/redis-stack:latest
    container_name: redis
    hostname: redis
    network_mode: host
    ports:
      - "${REDIS_PORT}:6379"
      - "${REDIS_INSIGHT_PORT}:8001"

  redis-init:
    image: redis/redis-stack:latest
    container_name: redis-init
    network_mode: host
    depends_on:
      - redis-metrics
    command: >
      sh -c '
        sleep 5;
        redis-cli -h localhost -p ${REDIS_PORT} FT.CREATE metricsIdx ON HASH PREFIX 1 metrics:phases: SCHEMA query TAG phase TAG approach TAG duration NUMERIC timestamp NUMERIC SORTABLE || true;
        echo "RediSearch index configured successfully!";
      '

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    hostname: grafana
    network_mode: host
    ports:
      - "${GRAFANA_PORT}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    volumes:
      - ./grafana/datasources:/etc/grafana/provisioning/datasources
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
